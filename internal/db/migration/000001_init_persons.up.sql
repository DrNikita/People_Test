CREATE TABLE people.public.people(
    id integer,
    name varchar(255),
    surname varchar(255),
    patronymic varchar(255),
    age integer,
    gender varchar(10),
    country_id varchar(5),
    primary key id
);